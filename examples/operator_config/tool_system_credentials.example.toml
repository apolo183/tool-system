# Copy this file outside the repository to ~/.config/tool-system/credentials.toml.
# Keep the copied directory mode 0700 and file mode 0600.
# Empty values are deliberately invalid; enter only keys owned by this installation.

[providers.deepseek]
api_key = ""

[providers.openai]
api_key = ""

[providers.qwen]
api_key = ""
