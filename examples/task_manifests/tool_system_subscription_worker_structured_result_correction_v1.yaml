task_id: tool-system-subscription-worker-structured-result-correction-v1
task_type: implementation
target_repo: apolo183/tool-system
target_branch: agent/subscription-worker-structured-result-correction-v1
phase: POST_P16_PRODUCT_COMPLETION
approved_blueprint_refs:
  - repo: apolo183/tool-system
    path: blueprint/tool_system_v0.yaml
    section_or_key: product_objective
alignment:
  parent:
    document: docs/reports/subscription_worker_public_entry_closure_specification_v1.md
    section_or_key: subscription worker interface
    scope: Return structured patch evidence to the dependent development loop.
  global:
    document: blueprint/tool_system_v0.yaml
    section_or_key: product_objective
    scope: Preserve bounded structured worker output and default-off APIs.
bounded_closure:
  frozen_before_execution:
    task_identity: SUBSCRIPTION-WORKER-STRUCTURED-RESULT-CORRECTION-v1
    baseline_commit: 55416af567317c9c08095fe3e081baeb9b7aace6
    baseline_tree: canonical_main_tree_at_55416af567317c9c08095fe3e081baeb9b7aace6
    allowed_scope: exact_5_paths_listed_below
    acceptance_set:
      - zero-exit newline-delimited JSON yields one terminal structured object
      - invalid JSON non-object timeout output limit and nonzero exit fail closed
      - raw process output is not retained
      - fake process only with no public-entry claim
    validation_set:
      - focused worker-adapter tests
      - full Hosted CI and governance validation
    terminal_predicate: structured result handoff is merged before public-entry integration resumes
    finite_budgets:
      implementation_cycles: 1
      repair_cycles: 2
      review_cycles: 1
      provider_invocations: 0
      credential_value_accesses: 0
      real_codex_invocations: 0
      real_downstream_repository_accesses: 0
      production_operations: 0
      cleanup_operations: 0
      rollback_operations: 0
  per_cycle_state: [candidate_tree, acceptance_digest, blocker_set, validation_results, attempt_number]
  recurrence_fingerprint: [task_identity, candidate_tree, acceptance_digest, blocker_set, validation_results]
  recurrence_fingerprint_excludes: [attempt_number, timestamps, receipts, status_text]
scope:
  summary: Correct the adapter's missing structured-result handoff.
  in_scope: &files
    - docs/reports/subscription_worker_structured_result_correction_v1.md
    - examples/change_plans/tool_system_subscription_worker_structured_result_correction_v1.yaml
    - examples/task_manifests/tool_system_subscription_worker_structured_result_correction_v1.yaml
    - src/tool_system/worker_adapter/contract.py
    - tests/test_worker_adapter_contract.py
  out_of_scope:
    - public CLI development loop local Git real Codex APIs credentials downstream production cleanup or rollback
allowed_files: *files
write_mode: pull_request
verification:
  commands:
    - git diff --check
    - PYTHONPATH=src PYTHONDONTWRITEBYTECODE=1 python -m pytest -q tests/test_worker_adapter_contract.py
    - PYTHONPATH=src PYTHONDONTWRITEBYTECODE=1 python -m pytest -q
publication:
  draft_pr_required: true
  ready_authorized: true
  squash_merge_authorized: true
  retain_feature_branch: true
  branch_deletion_authorized: false
  merge_requires_unchanged_base_head_scope_ci_comments_and_reviews: true
rollback:
  method: git_revert
  reference: tool-system@55416af567317c9c08095fe3e081baeb9b7aace6
  execution_authorized: false
approval:
  required: true
  approved_by: user
  approval_source: authorized_milestone_routine_correction
  approved_at: 2026-08-10 UTC+09:00
